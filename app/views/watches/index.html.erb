<section class="watches">

	<% @watches.each do |watch| %>

		<div class="watch"> 
			<%= link_to watch do %>
			<%= image_tag watch.image.url(:medium) %>
			<!-- IMAGE -->
				<%= link_to new_watch_order_path(watch), data: { no_turbolink: true } do %>
					<!-- PRICE -->
					<div class="price">
						<span><%= number_to_currency watch.price, unit: "£" %></span>
						
					</div>
				<% end %>
			<% end %>

			<h2><%= watch.name %></h2>

		</div>
	
	<% end %>


</section>